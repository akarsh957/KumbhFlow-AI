# Server configuration
PORT=8081

# MongoDB Atlas connection string (MERN stack database)
# Replace with your actual username, password, and cluster details
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/kumbhflow?retryWrites=true&w=majority
